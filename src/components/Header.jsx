function Header({titulo, description}){
    return (
        <div>
            <h1>{titulo}</h1>
            <p>{description}</p>
        </div>
    )
    
}

//Export Named
export {Header};

/* export default Header;
// Export Default */